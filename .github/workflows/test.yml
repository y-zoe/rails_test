name: Test Script
on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - 'master'
jobs:
  check:
    name: check test
    runs-on: ubuntu-latest
    steps:
      - name: check test
        run: echo "Hello World"
